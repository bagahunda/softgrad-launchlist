.footer
  z-index: 2
  position fixed
  bottom -2px
  left 50%
  width 90%
  max-width: 60em
  transform: translateX(-50%);
  padding 1em
  border-radius: 4px
  box-shadow: 0 15px 35px rgba(50,50,93,.1),0 5px 15px rgba(0,0,0,.07)
  background #ffffff
